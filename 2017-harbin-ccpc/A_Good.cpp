/*
 *思路见Note
 */
#include <bits/stdc++.h>
using namespace std;
const int maxn=;
int N,p[maxn];
char str[maxn];
void get_par(char *str){

}
int main()
{
	int T;
	scanf("%d",&T);
	while(T--)
	{
		scanf("%s",str);
		int len=strlen(str);
		get_par(str);
	}
}
