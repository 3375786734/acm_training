#include <bits/stdc++.h>
#define MEM(a,num) memset(a,num,sizeof(a))
#define rep(i,a,b) for(int i=a;i<=b;i++)
using namespace std;
typedef long long ll;
const int maxn=500;
int sum[maxn][maxn],num[maxn];
int main()
{
	int T;
	scanf("%d",&T);
	while(T--)
	{
		scanf("%d",&L);
		ll unit;
		rep(i,1,L)scanf("%d",&num[i]);

		scanf("%d",&q);
		rep(i,1,q)
			scanf("%d%d%d%d",&x1,&y1,&x2,&y2);
		
	}
}
