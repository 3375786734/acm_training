/*idea:注意到从凸包表面紧邻的三个点切下来之后仍然是凸包,因此按照这个顺序进行
 *不停的递归切分即可
 *
 *复杂度分析:由于是动态维护凸包,因此需要
 *
 *基础知识:怎么构造
 */
#include <bits/stdc++.h>
using namespace std;

int main()
{
	int T;
	scanf("%d",&T);
	while(T--)
	{
		scanf("%d",&N);
		for(int i=1;i<=N;i++)
			scanf("%d%d",&a,&b);

	}	
}
