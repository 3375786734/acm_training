#include <bits/stdc++.h>
#define MEM(a,num) memset(a,num,sizeof(a))
#define rep(i,a,b) for(int i=a;i<=b;i++)
using namespace std;
//const int maxn=;
int main()
{
	int T,cas=1;
	scanf("%d",&T);
	while(T--)
	{
		int N;
		scanf("%d",&N);
		printf("Case #%d: ",cas++);
		printf("%d\n",N/3);
	}
}
